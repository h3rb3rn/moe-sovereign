// Placeholder used only during image build to prime the Cargo dependency
// cache for `loom` (see Dockerfile). Every real request overwrites this file
// with the submitted source before running `cargo test --offline`.
