# --- LangChain & LangGraph ---
langchain>=1.3.9
langchain-openai
langchain-community
langchain-chroma
langgraph>=0.4.0
langgraph-checkpoint-postgres>=3.1.1
psycopg[binary,pool]>=3.2.0

# --- API & Server ---
fastapi>=0.115.0
uvicorn[standard]
pydantic>=2.0.0
python-multipart>=0.0.31
python-dotenv

# --- Datenbanken & Mathe ---
redis>=5.0.0
chromadb>=1.5.9  # CVE-2026-45829 (pre-auth code injection, ALL >=1.0.0 affected, no patch yet) plus two RBAC advisories
                 # (SimpleRBACAuthorizationProvider does not scope permissions to a tenant/database/collection; an
                 # authenticated caller can read/write/delete across tenants). None fixed by this version pin — all
                 # three are mitigated at the network layer instead: no host port, chroma_internal is `internal: true`
                 # (egress-blocked), and the orchestrator is the only client with network reach to the server at all
                 # (see docker-compose.yml chromadb-vector). Revisit once upstream ships patched releases.
numpy>=1.26.0,<2.0.0
sympy==1.12
neo4j>=5.0.0

# --- Web & Networking ---
beautifulsoup4
httpx
aiohttp
ddgs  # DuckDuckGo fallback for web_search.py when SearXNG is unavailable/degraded

# --- Kafka ---
aiokafka>=0.11.0

# --- Monitoring ---
prometheus_client>=0.21.0

# --- OIDC/JWT ---
PyJWT>=2.13.0
cryptography>=50.0.0

# --- PDF generation & parsing ---
weasyprint>=62.0
pypdf>=6.16.2

# --- System Constitution enforcement ---
pyyaml>=6.0

# --- Pinned transitive deps (guard against unwanted bumps) ---
urllib3==2.7.0